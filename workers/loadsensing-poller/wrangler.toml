name = "loadsensing-poller"
main = "src/worker.js"
compatibility_date = "2025-09-01"

# Cron: every 30 minutes.
[triggers]
crons = ["*/30 * * * *"]

# Secrets are set with `wrangler secret put NAME` — do NOT commit them.
# Required:
#   LS_USER          → "admin"
#   LS_PASS          → "quoh7say4"
#   SUPABASE_URL     → "https://dhddckamrkfleuigrsip.supabase.co"   (Phase 2)
#   SUPABASE_SR_KEY  → service role key                              (Phase 2)
#
# Public env vars (no secret), if you want to override defaults:
# [vars]
# GATEWAY_ID = "27808"
